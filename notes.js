// this didn't work, not sure what was going on
//  "start":"babel . --out-dir compiled --presets=react --ignore=node_modules,compiled --source-maps inline --watch && live-server"

// this seemed to work, but might have created issues, so I seperated them into two commands and ran them in two seperate terminals
// "start":"live-server & babel . --out-dir compiled --presets=react --ignore=node_modules,compiled --source-maps inline --watch"

//
//

